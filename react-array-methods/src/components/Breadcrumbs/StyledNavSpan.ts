import styled from 'styled-components';

const StyledNavSpan = styled.span`
  font-size: 24px;
  font-family: 'Raleway';
  color: rgb(82, 81, 84);
  font-weight: bold;
  line-height: 1.2;
`;

export default StyledNavSpan;
